<!DOCTYPE html>
<html lang="en">
<head>
    <title>MongoShop</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="description" content="Wish shop project">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <partial name="CustomerTemplate/_Styles" />
</head>

<body>

    @*<div class="super_container">*@
    <!-- Header -->
    <partial name="CustomerTemplate/_Headers" />
    @*@await Html.PartialAsync("CustomerTemplate/_Headers", Model.ListProduct[0])*@


    <!-- Menu -->
    <!--<partial name="CustomerTemplate/_Menus" />-->
    <!-- New Arrivals -->
    <!--<div class="arrivals">
        <div class="container">
            <div class="row">
                <div class="col">
                    <div class="section_title_container text-center">
                        <div class="section_subtitle">only the best</div>
                        <div class="section_title">new arrivals</div>
                    </div>
                </div>
            </div>
            <div class="row products_container">-->
    <!-- Product -->
    @RenderBody()



    <!-- Extra -->
    <!-- Footer -->
    <partial name="CustomerTemplate/_Footers" />


    @*</div>*@

    <partial name="CustomerTemplate/_Scripts" />
    <script>

        $(document).ready((element) =>{
            $.get('@Url.Action("GetCurrentCartCount", "Cart")', function(content){
                        $("#lblCartCount").html(content);
            });
        });
        
        // first case
        $('.drop-files-container').on('drop dragdrop', function (e) {
            //var files = e.originalEvent.dataTransfer.files;
            processFileUpload();
            //return false;
        });

        // second case
        $('#imageUpload').on('change', function (e) {
            //    var files = document.getElementById('files').files;
            processFileUpload();
            return false;
        });

        function processFileUpload() {
            document.getElementById('imageSearchForm').submit();
        }

        $('.drop-files-container').on('dragenter', function (event) {
            event.preventDefault();
            $(this).css('background', '#f1f1f1');
        })
        $('.drop-files-container').on('dragover', function (event) {
            event.preventDefault();
        })
    </script>>
    @RenderSection("Scripts", false)
</body>
</html>